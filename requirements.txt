# ============================================================
#  JARVIS v2.0 — Python Dependencies
#  Install: pip install -r requirements.txt
# ============================================================

# Google Gemini AI SDK (terbaru — menggantikan google-generativeai)
google-genai>=1.0.0

# Speech Recognition (STT)
SpeechRecognition>=3.10.0

# PyAudio — required by SpeechRecognition for mic input
# Windows: pip install pipwin && pipwin install pyaudio
# atau download wheel: https://www.lfd.uci.edu/~gohlke/pythonlibs/
pyaudio>=0.2.13

# Google Text-to-Speech (TTS)
gTTS>=2.3.2

# Audio playback untuk TTS output
pygame>=2.5.0

# Groq AI SDK (online LLM dengan free tier)
groq>=0.9.0

# Environment variable loader
python-dotenv>=1.0.0
Flask>=3.0.0
