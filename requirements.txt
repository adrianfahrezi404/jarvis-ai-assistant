# ============================================================
#  JARVIS v2.0 — Python Dependencies (Web / Vercel)
#  Install: pip install -r requirements.txt
#
#  Untuk versi Desktop (Tkinter + Audio), install juga:
#  pip install -r requirements-desktop.txt
# ============================================================

# Google Gemini AI SDK
google-genai>=1.0.0

# Groq AI SDK (online LLM dengan free tier)
groq>=0.9.0

# Environment variable loader
python-dotenv>=1.0.0

# Web framework (untuk Vercel Serverless)
Flask>=3.0.0
